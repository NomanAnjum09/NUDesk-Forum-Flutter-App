class Posts {
  String username;
  DateTime postedOn;
  String text;
  String description;
  String course;

  Posts(
      {this.course, this.description, this.postedOn, this.text, this.username});
}
